[project]
name = "grid-aware-battery"
version = "0.1.0"
description = "Grid frequency model with AGC secondary control, battery arbitrage MPC, and frequency-aware coupling"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
